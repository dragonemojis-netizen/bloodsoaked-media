import { NotFoundBody } from "@/components/layout/NotFoundBody";

/** Hub 404 — parent (hub) layout already provides publication chrome. */
export default function ArchivesHubNotFound() {
  return <NotFoundBody />;
}
