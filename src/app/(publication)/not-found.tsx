import { NotFoundBody } from "@/components/layout/NotFoundBody";

/** 404 inside publication chrome tree — parent layout already wraps chrome. */
export default function PublicationNotFound() {
  return <NotFoundBody />;
}
