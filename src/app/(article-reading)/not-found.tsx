import { NotFoundBody } from "@/components/layout/NotFoundBody";

/** 404 inside article-reading chrome tree — parent layout already wraps chrome. */
export default function ArticleReadingNotFound() {
  return <NotFoundBody />;
}
